import React from 'react';
import { render, screen } from '@testing-library/react';
import '@testing-library/jest-dom/extend-expect';
import LoginOwnerForm from './LoginOwnerForm';

/*
describe('<LoginOwnerForm />', () => {
  test('it should mount', () => {
    render(<LoginOwnerForm />);
    
    const loginOwnerForm = screen.getByTestId('LoginOwnerForm');

    expect(loginOwnerForm).toBeInTheDocument();
  });
});*/